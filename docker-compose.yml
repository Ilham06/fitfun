version: "3.9"

services:
  portfolio:
    container_name: fitfun
    image: ilhammuhamad/fitfun
    ports:
      - "4001:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
